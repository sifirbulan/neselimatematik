function findQuickButton(label){return [...document.querySelectorAll('.quickActions button')].find(b=>(b.textContent||'').includes(label))}
function ensureComposerTools(){const actions=document.querySelector('.questionBox .composerActions');if(!actions)return;if(actions.querySelector('.composerInlineTools'))return;const solve=actions.querySelector('.solveButton');if(!solve)return;const wrap=document.createElement('div');wrap.className='composerInlineTools';const photo=document.createElement('button');photo.type='button';photo.className='composerToolButton';photo.innerHTML='<span>📷</span><b>Fotoğraf</b>';photo.addEventListener('click',()=>findQuickButton('Fotoğrafla Sor')?.click());const voice=document.createElement('button');voice.type='button';voice.className='composerToolButton';voice.innerHTML='<span>🎙️</span><b>Sesli Sor</b>';voice.addEventListener('click',()=>findQuickButton('Sesli Sor')?.click()||findQuickButton('Dinliyorum')?.click());wrap.append(photo,voice);actions.insertBefore(wrap,solve)}
function updateBrandSubtitle(){const subtitle=document.querySelector('.brandMark small');if(subtitle&&subtitle.textContent?.includes('Matematik'))subtitle.textContent="Neş'eli Yapay Zekâ Öğrenme Asistanı"}
function refreshUi(){ensureComposerTools();updateBrandSubtitle()}
const observer=new MutationObserver(()=>refreshUi());observer.observe(document.documentElement,{childList:true,subtree:true});refreshUi();
import React, { useEffect, useRef, useState } from "react";
import { createRoot } from "react-dom/client";
import { PhotoCropper, type Crop } from "./PhotoCropper";
import { SolutionResult } from "./SolutionResult";
import { getSubjectsForGrade, getTopicsForGrade, gradeLevels } from "./curriculum";
import type { SolveResponse } from "./types";
import "./styles.css";
import "./hint.css";
import "./learning.css";

const configuredApiUrl=(import.meta.env.VITE_API_URL??"").trim().replace(/\/+$/,"");
const API_URL=!configuredApiUrl||configuredApiUrl==="https://nesevren-api.onrender.com"?"https://nesevren-api-v2.onrender.com":configuredApiUrl;
const subjects=["Otomatik","Matematik","Fen Bilimleri","Fizik","Kimya","Biyoloji","Türkçe","Türk Dili ve Edebiyatı","İngilizce","Kürtçe","Sosyal Bilgiler","Tarih","Coğrafya","Felsefe"];
const grades=[...gradeLevels];

type HistoryItem={question:string;subject:string;createdAt:number};
type PracticeQuestion={question:string;options:string[];correctIndex:number;hint:string};
type PracticeFeedback={selected:number;correct:boolean};
type DiagnosticQuestion=PracticeQuestion&{topic:string};
type TopicStat={correct:number;wrong:number};
type AssessmentResult={subject:string;grade:string;topic:string;score:number;total:number;level:string;weakTopics:string[];topicStats:Record<string,TopicStat>;createdAt:number};
type ErrorBookItem={id:string;subject:string;grade:string;topic:string;question:string;options:string[];correctIndex:number;hint:string;mistakeCount:number;retryCorrect:number;retryWrong:number;createdAt:number;lastSeenAt:number};

declare global { interface Window { webkitSpeechRecognition?: new()=>any; SpeechRecognition?: new()=>any; } }

function cleanJson(raw:string){return raw.trim().replace(/^```(?:json)?/i,"").replace(/```$/i,"").trim()}
function assessmentLevelLabel(grade:string){return /^\d+$/.test(grade)?`${grade}. sınıf`:grade}
function optionCountForGrade(grade:string){if(grade==="LGS")return 4;const n=Number(grade);if(Number.isFinite(n)){if(n<=4)return 3;if(n<=8)return 4;}return 5}
function questionCountForGrade(grade:string){if(grade==="LGS")return 20;const n=Number(grade);if(Number.isFinite(n)){if(n<=4)return 10;if(n<=8)return 20;}return 40}
function getAssessmentTopics(subject:string,grade:string){return getTopicsForGrade(subject,grade)}
function storedCoachLevel(){try{const raw=localStorage.getItem("nesevren-coach-profile");if(!raw)return null;const profile=JSON.parse(raw) as {level?:unknown};const level=typeof profile.level==="string"?profile.level.trim():"";return (grades as readonly string[]).includes(level)?level:null}catch{return null}}
function parsePracticeQuestions(raw:string,expectedOptions=4):PracticeQuestion[]{const value=JSON.parse(cleanJson(raw)) as unknown;if(!Array.isArray(value))throw new Error("Alıştırma soruları uygun biçimde hazırlanamadı.");const parsed=value.slice(0,3).map((item,index)=>{if(!item||typeof item!=="object")throw new Error(`${index+1}. alıştırma sorusu geçersiz.`);const q=item as Record<string,unknown>;if(typeof q.question!=="string"||!Array.isArray(q.options)||q.options.length!==expectedOptions||!q.options.every(o=>typeof o==="string")||typeof q.correctIndex!=="number"||!Number.isInteger(q.correctIndex)||q.correctIndex<0||q.correctIndex>=q.options.length||typeof q.hint!=="string")throw new Error(`${index+1}. alıştırma sorusunda eksik bilgi var.`);return{question:q.question,options:q.options as string[],correctIndex:q.correctIndex,hint:q.hint}});if(parsed.length!==3)throw new Error("Tam olarak 3 alıştırma sorusu üretilemedi.");return parsed}
function parseDiagnosticQuestions(raw:string,expectedOptions:number,expectedQuestions:number):DiagnosticQuestion[]{const value=JSON.parse(cleanJson(raw)) as unknown;if(!Array.isArray(value))throw new Error("Seviye soruları uygun biçimde hazırlanamadı.");const parsed=value.slice(0,expectedQuestions).map((item,index)=>{if(!item||typeof item!=="object")throw new Error(`${index+1}. seviye sorusu geçersiz.`);const q=item as Record<string,unknown>;if(typeof q.question!=="string"||!Array.isArray(q.options)||q.options.length!==expectedOptions||!q.options.every(o=>typeof o==="string")||typeof q.correctIndex!=="number"||!Number.isInteger(q.correctIndex)||q.correctIndex<0||q.correctIndex>=expectedOptions||typeof q.hint!=="string"||typeof q.topic!=="string")throw new Error(`${index+1}. seviye sorusu ${expectedOptions} şıklı hazırlanmadı.`);return{question:q.question,options:q.options as string[],correctIndex:q.correctIndex,hint:q.hint,topic:q.topic}});if(parsed.length!==expectedQuestions)throw new Error(`Tam olarak ${expectedQuestions} seviye sorusu üretilemedi.`);return parsed}
function levelFromScore(score:number,total:number){const ratio=total?score/total:0;if(ratio<0.4)return"Başlangıç";if(ratio<0.7)return"Gelişiyor";if(ratio<0.9)return"İyi";return"Çok iyi"}
function errorBookId(subject:string,grade:string,topic:string,question:string){return`${subject}|${grade}|${topic}|${question}`}

function App(){
 const [question,setQuestion]=useState(""); const [subject,setSubject]=useState("Otomatik"); const [showSubject,setShowSubject]=useState(false); const [result,setResult]=useState<SolveResponse|null>(null); const [hintText,setHintText]=useState(""); const [practiceQuestions,setPracticeQuestions]=useState<PracticeQuestion[]>([]); const [practiceFeedback,setPracticeFeedback]=useState<Record<number,PracticeFeedback>>({}); const [celebration,setCelebration]=useState<number|null>(null); const [verifyOpen,setVerifyOpen]=useState(false); const [studentAnswer,setStudentAnswer]=useState(""); const [verifyText,setVerifyText]=useState(""); const [error,setError]=useState(""); const [loading,setLoading]=useState(false); const [hintLoading,setHintLoading]=useState(false); const [similarLoading,setSimilarLoading]=useState(false); const [verifyLoading,setVerifyLoading]=useState(false); const [teacherOpen,setTeacherOpen]=useState(false); const [teacherSent,setTeacherSent]=useState(false); const [voiceActive,setVoiceActive]=useState(false); const [history,setHistory]=useState<HistoryItem[]>([]); const [assessmentOpen,setAssessmentOpen]=useState(false); const [assessmentSubject,setAssessmentSubject]=useState("Matematik"); const [assessmentGrade,setAssessmentGrade]=useState("5"); const [assessmentTopic,setAssessmentTopic]=useState("Genel Tarama"); const [assessmentLoading,setAssessmentLoading]=useState(false); const [assessmentQuestions,setAssessmentQuestions]=useState<DiagnosticQuestion[]>([]); const [assessmentAnswers,setAssessmentAnswers]=useState<Record<number,number>>({}); const [assessmentResult,setAssessmentResult]=useState<AssessmentResult|null>(null); const [errorBook,setErrorBook]=useState<ErrorBookItem[]>([]); const [errorBookOpen,setErrorBookOpen]=useState(false); const [errorRetryId,setErrorRetryId]=useState<string|null>(null); const [errorRetrySelected,setErrorRetrySelected]=useState<number|null>(null); const [errorPracticeLoading,setErrorPracticeLoading]=useState(false); const [photoName,setPhotoName]=useState(""); const [photoPreview,setPhotoPreview]=useState(""); const [originalPhoto,setOriginalPhoto]=useState(""); const [cropSource,setCropSource]=useState(""); const [lastCrop,setLastCrop]=useState<Crop|undefined>(); const photoInput=useRef<HTMLInputElement>(null); const questionInput=useRef<HTMLTextAreaElement>(null);
 const subjectLabel=subject==="Otomatik"?"Dersi AI algılar":subject; const busy=loading||hintLoading||similarLoading||verifyLoading||assessmentLoading||errorPracticeLoading; const assessmentOptionCount=optionCountForGrade(assessmentGrade); const assessmentQuestionCount=questionCountForGrade(assessmentGrade); const assessmentSubjects=getSubjectsForGrade(assessmentGrade,subjects); const practiceGrade=assessmentResult?.grade??storedCoachLevel(); const practiceOptionCount=optionCountForGrade(practiceGrade??"5"); const practiceLevelText=practiceGrade?assessmentLevelLabel(practiceGrade):"Standart seviye"; const errorRetryItem=errorBook.find(x=>x.id===errorRetryId)??null; const errorGroups=Object.entries(errorBook.reduce<Record<string,ErrorBookItem[]>>((acc,item)=>{const key=`${item.subject} · ${/^\d+$/.test(item.grade)?`${item.grade}. sınıf`:item.grade} · ${item.topic}`;(acc[key]??=[]).push(item);return acc},{}));
 function persistErrorBook(next:ErrorBookItem[]){setErrorBook(next);try{localStorage.setItem("nesevren-error-book",JSON.stringify(next));}catch{}}
 useEffect(()=>{try{const raw=localStorage.getItem("nesevren-history");if(raw)setHistory(JSON.parse(raw));const a=localStorage.getItem("nesevren-assessment");if(a)setAssessmentResult(JSON.parse(a));const e=localStorage.getItem("nesevren-error-book");if(e)setErrorBook(JSON.parse(e));}catch{}},[]);
 useEffect(()=>{if(celebration===null)return;const t=window.setTimeout(()=>setCelebration(null),1800);return()=>window.clearTimeout(t)},[celebration]);
 useEffect(()=>{if(!assessmentSubjects.includes(assessmentSubject))setAssessmentSubject(assessmentSubjects[0]??"Matematik")},[assessmentGrade]);
 useEffect(()=>{const topics=getAssessmentTopics(assessmentSubject,assessmentGrade);if(!topics.includes(assessmentTopic))setAssessmentTopic(topics[0]??"Genel Tarama")},[assessmentSubject,assessmentGrade]);
 useEffect(()=>{if(assessmentQuestions.length!==assessmentQuestionCount||Object.keys(assessmentAnswers).length!==assessmentQuestionCount)return;const score=assessmentQuestions.reduce((sum,item,index)=>sum+(assessmentAnswers[index]===item.correctIndex?1:0),0);const weak=[...new Set(assessmentQuestions.filter((item,index)=>assessmentAnswers[index]!==item.correctIndex).map(item=>item.topic))];const topicStats:Record<string,TopicStat>={};assessmentQuestions.forEach((item,index)=>{const stat=topicStats[item.topic]??{correct:0,wrong:0};if(assessmentAnswers[index]===item.correctIndex)stat.correct+=1;else stat.wrong+=1;topicStats[item.topic]=stat});const nextResult={subject:assessmentSubject,grade:assessmentGrade,topic:assessmentTopic,score,total:assessmentQuestionCount,level:levelFromScore(score,assessmentQuestionCount),weakTopics:weak,topicStats,createdAt:Date.now()};setAssessmentResult(nextResult);try{localStorage.setItem("nesevren-assessment",JSON.stringify(nextResult));}catch{}const now=Date.now();setErrorBook(prev=>{const next=[...prev];assessmentQuestions.forEach((item,index)=>{if(assessmentAnswers[index]===item.correctIndex)return;const id=errorBookId(assessmentSubject,assessmentGrade,item.topic,item.question);const found=next.find(x=>x.id===id);if(found){found.mistakeCount+=1;found.lastSeenAt=now}else next.unshift({id,subject:assessmentSubject,grade:assessmentGrade,topic:item.topic,question:item.question,options:item.options,correctIndex:item.correctIndex,hint:item.hint,mistakeCount:1,retryCorrect:0,retryWrong:0,createdAt:now,lastSeenAt:now})});try{localStorage.setItem("nesevren-error-book",JSON.stringify(next));}catch{}return next})},[assessmentAnswers,assessmentQuestions,assessmentSubject,assessmentGrade,assessmentTopic,assessmentQuestionCount]);
 function saveHistory(text:string,detectedSubject?:string){const trimmed=text.trim();if(!trimmed)return;const actual=detectedSubject&&detectedSubject!=="Genel"?detectedSubject:subject;const item={question:trimmed.slice(0,180),subject:actual,createdAt:Date.now()};const next=[item,...history.filter(h=>h.question!==item.question)].slice(0,6);setHistory(next);try{localStorage.setItem("nesevren-history",JSON.stringify(next));}catch{}}
 async function requestAI(intent:"solve"|"hint",extra=""){const value=question.trim();if(!value&&!photoPreview){setError("Lütfen sorunuzu yazın veya bir soru fotoğrafı seçin.");return null;}const isImageQuestion=Boolean(photoPreview);const selectedSubject=subject==="Otomatik"?"Kullanıcı ders seçmedi.":`Kullanıcının seçtiği ders: ${subject}. Bu seçim yalnızca bir ipucudur ve yanlış olabilir.`;const enriched=`${selectedSubject} Her durumda sorunun gerçek dersini, konusunu ve seviyesini sorunun içeriğinden bağımsız olarak yeniden belirle. Seçim ile içerik çelişirse gerçek dersi esas al. Öğrencinin seviyesine uygun, açık ve öğretici anlat. ${extra} ${value}`.trim();const response=await fetch(`${API_URL}/api/v1/questions/analyze`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({question:enriched,inputType:isImageQuestion?"image":"text",intent,...(isImageQuestion?{imageDataUrl:photoPreview}:{})})});const data=await response.json().catch(()=>null);if(!response.ok)throw new Error(data?.error?.message??`Yapay zekâ servisi ${response.status} hatası döndürdü.`);return data as SolveResponse}
 function getAnswerText(data:SolveResponse|null){const a=data?.finalAnswer??data?.answer;return a?`${a.answer}${a.explanation?`\n${a.explanation}`:""}`:""}
 async function submitQuestion(){setLoading(true);setError("");setHintText("");setPracticeQuestions([]);setPracticeFeedback({});setVerifyText("");setResult(null);try{const data=await requestAI("solve");if(data){setResult(data);saveHistory(question||photoName||"Fotoğraflı soru",data.analysis?.topic)}}catch(e){setError(e instanceof Error?e.message:"Beklenmeyen bir hata oluştu.")}finally{setLoading(false)}}
 async function requestHint(){setHintLoading(true);setError("");setHintText("");try{const data=await requestAI("hint","Cevabı veya doğru şıkkı söyleme. Yalnızca ilk doğru adıma götüren kısa ipucu ver.");const hint=data?.finalAnswer?.hint||data?.answer?.hint||data?.finalAnswer?.explanation||data?.answer?.explanation;if(!hint)throw new Error("Bu soru için ipucu üretilemedi.");setHintText(hint)}catch(e){setError(e instanceof Error?e.message:"İpucu hazırlanamadı.")}finally{setHintLoading(false)}}
 async function requestSimilar(){setSimilarLoading(true);setError("");setPracticeQuestions([]);setPracticeFeedback({});try{const latestPracticeGrade=assessmentResult?.grade??storedCoachLevel();const optionCount=optionCountForGrade(latestPracticeGrade??"5");const levelText=latestPracticeGrade?assessmentLevelLabel(latestPracticeGrade):"seviye bilgisi henüz kaydedilmemiş öğrenci";const optionLabels=Array.from({length:optionCount},(_,i)=>String.fromCharCode(65+i)).join(", ");const data=await requestAI("solve",`Öğrencinin düzeyi: ${levelText}. Orijinal sorunun gerçek dersini ve aynı beceriyi esas al. TAM OLARAK 3 yeni çoktan seçmeli soru üret. Her soruda TAM OLARAK ${optionCount} şık (${optionLabels}) olsun. 1-4. sınıflarda 3 şık; 5-8. sınıflar ve LGS'de 4 şık; 9-12. sınıflar ile TYT, AYT, KPSS ve ALES'te 5 şık kuralına uy. Seviye bilgisi yoksa standart 4 şık kullan. Sorular öğrencinin düzeyine uygun başlayıp üçüncü soruda biraz zorlaşsın. answer alanına yalnızca geçerli JSON dizi yaz: [{"question":"...","options":[...],"correctIndex":0,"hint":"doğru cevabı açık etmeyen kısa ipucu"}, ...].`);const raw=(data?.finalAnswer??data?.answer)?.answer;if(!raw)throw new Error("Benzer sorular üretilemedi.");setPracticeQuestions(parsePracticeQuestions(raw,optionCount))}catch(e){setError(e instanceof Error?e.message:"Benzer sorular hazırlanamadı.")}finally{setSimilarLoading(false)}}
 async function generateErrorSimilar(item:ErrorBookItem){setErrorPracticeLoading(true);setError("");setPracticeQuestions([]);setPracticeFeedback({});try{const optionCount=optionCountForGrade(item.grade);const prompt=`${item.subject} dersi, ${assessmentLevelLabel(item.grade)} seviyesi, ${item.topic} konusu. Öğrencinin daha önce yanlış yaptığı soru: ${item.question}. Aynı kazanımı ölçen fakat sayı, bağlam veya ifade biçimi değiştirilmiş TAM OLARAK 3 yeni çoktan seçmeli soru üret. Her soruda TAM OLARAK ${optionCount} şık olsun. Sorular aynı zorluk düzeyinden başlayıp biraz zorlaşsın. answer alanına yalnızca geçerli JSON dizi yaz: [{"question":"...","options":[...],"correctIndex":0,"hint":"cevabı söylemeyen kısa ipucu"}, ...].`;const response=await fetch(`${API_URL}/api/v1/questions/analyze`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({question:prompt,inputType:"text",intent:"generate_test"})});const data=await response.json().catch(()=>null) as SolveResponse|null;if(!response.ok)throw new Error((data as any)?.error?.message??"Benzer hata soruları hazırlanamadı.");const raw=(data?.finalAnswer??data?.answer)?.answer;if(!raw)throw new Error("Benzer hata soruları üretilemedi.");setPracticeQuestions(parsePracticeQuestions(raw,optionCount));setQuestion(item.question);setSubject(item.subject);setErrorBookOpen(false);setErrorRetryId(null);window.setTimeout(()=>window.scrollTo({top:0,behavior:"smooth"}),50)}catch(e){setError(e instanceof Error?e.message:"Benzer hata soruları hazırlanamadı.")}finally{setErrorPracticeLoading(false)}}
 function selectPracticeOption(questionIndex:number,optionIndex:number){if(practiceFeedback[questionIndex])return;const item=practiceQuestions[questionIndex];if(!item)return;const correct=optionIndex===item.correctIndex;setPracticeFeedback(prev=>({...prev,[questionIndex]:{selected:optionIndex,correct}}));if(correct)setCelebration(questionIndex)}
 function retryErrorAnswer(optionIndex:number){if(!errorRetryItem||errorRetrySelected!==null)return;setErrorRetrySelected(optionIndex);const correct=optionIndex===errorRetryItem.correctIndex;const next=errorBook.map(item=>item.id===errorRetryItem.id?{...item,retryCorrect:item.retryCorrect+(correct?1:0),retryWrong:item.retryWrong+(correct?0:1),lastSeenAt:Date.now()}:item);persistErrorBook(next);if(correct)setCelebration(999)}
 async function verifyStudentAnswer(){if(!studentAnswer.trim()){setError("Önce kendi cevabınızı veya çözümünüzü yazın.");return}setVerifyLoading(true);setError("");setVerifyText("");try{const data=await requestAI("solve",`Öğrencinin cevabı: ${studentAnswer.trim()}. Doğruysa kısa gerekçe ver; yanlışsa doğru cevabı söylemeden ilk hata noktasını açıkla.`);setVerifyText(getAnswerText(data)||"Doğrulama sonucu alınamadı.")}catch(e){setError(e instanceof Error?e.message:"Doğrulama yapılamadı.")}finally{setVerifyLoading(false)}}
 async function startAssessment(){setAssessmentLoading(true);setError("");setAssessmentQuestions([]);setAssessmentAnswers({});setAssessmentResult(null);try{const optionLabels=Array.from({length:assessmentOptionCount},(_,i)=>String.fromCharCode(65+i)).join(", ");const levelLabel=assessmentLevelLabel(assessmentGrade);const focus=assessmentTopic==="Genel Tarama"?`${levelLabel} ${assessmentSubject} programının temel tema/ünitelerini dengeli biçimde tara.`:`${levelLabel} ${assessmentSubject} programında yer alan ${assessmentTopic} tema/ünitesini ölç.`;const prompt=`${assessmentSubject} dersi, ${levelLabel} seviyesi için seviye belirleme testi hazırla. ${focus} Bu seviyenin kapsamı dışındaki konuları sorma. TAM OLARAK ${assessmentQuestionCount} çoktan seçmeli soru üret. Bu seviye için HER SORUDA TAM OLARAK ${assessmentOptionCount} ŞIK (${optionLabels}) bulunmalı. 1-4. sınıflarda 3 şık ve 10 soru; 5-8. sınıflarda 4 şık ve 20 soru; LGS'de 4 şık ve 20 soru; 9-12. sınıflar ile TYT, AYT, KPSS ve ALES'te 5 şık ve 40 soru kuralına kesinlikle uy. Sorular kolaydan zora ilerlesin ve öğrencinin gerçek konu düzeyini ayırt etsin. answer alanına yalnızca geçerli JSON dizi yaz. Her nesne {"question":"...","options":[...],"correctIndex":0,"hint":"yanlışta cevabı söylemeden kısa ipucu","topic":"ölçülen alt konu veya beceri"} biçiminde olsun ve toplam ${assessmentQuestionCount} nesne bulunsun. correctIndex 0-${assessmentOptionCount-1} arasında olsun.`;const response=await fetch(`${API_URL}/api/v1/questions/analyze`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({question:prompt,inputType:"text",intent:"generate_test"})});const data=await response.json().catch(()=>null) as SolveResponse|null;if(!response.ok)throw new Error((data as any)?.error?.message??"Seviye testi hazırlanamadı.");const raw=(data?.finalAnswer??data?.answer)?.answer;if(!raw)throw new Error("Seviye soruları üretilemedi.");setAssessmentQuestions(parseDiagnosticQuestions(raw,assessmentOptionCount,assessmentQuestionCount))}catch(e){setError(e instanceof Error?e.message:"Seviye testi hazırlanamadı.")}finally{setAssessmentLoading(false)}}
 function chooseAssessmentAnswer(questionIndex:number,optionIndex:number){if(assessmentAnswers[questionIndex]!==undefined)return;setAssessmentAnswers(prev=>({...prev,[questionIndex]:optionIndex}));const item=assessmentQuestions[questionIndex];if(item&&optionIndex===item.correctIndex)setCelebration(100+questionIndex)}
 function resetAssessment(){setAssessmentQuestions([]);setAssessmentAnswers({});setAssessmentResult(null)}
 function startVoice(){const Speech=window.SpeechRecognition||window.webkitSpeechRecognition;if(!Speech){setError("Bu tarayıcı sesli soru özelliğini desteklemiyor. Chrome ile tekrar deneyebilirsiniz.");return}try{const r=new Speech();r.lang=subject==="İngilizce"?"en-US":"tr-TR";r.interimResults=false;r.maxAlternatives=1;r.onstart=()=>{setVoiceActive(true);setError("")};r.onend=()=>setVoiceActive(false);r.onerror=()=>{setVoiceActive(false);setError("Ses algılanamadı. Lütfen tekrar deneyin.")};r.onresult=(e:any)=>{const text=e.results?.[0]?.[0]?.transcript?.trim();if(text){setQuestion(q=>q?`${q} ${text}`:text);setTimeout(()=>questionInput.current?.focus(),20)}};r.start()}catch{setError("Mikrofon başlatılamadı. Tarayıcı mikrofon iznini kontrol edin.")}}
 function choosePhoto(){photoInput.current?.click()} function onPhotoChange(e:React.ChangeEvent<HTMLInputElement>){const file=e.target.files?.[0];if(!file)return;if(!file.type.startsWith("image/")){setError("Lütfen galeriden veya dosyalardan bir fotoğraf seçin.");return}if(file.size>8*1024*1024){setError("Fotoğraf en fazla 8 MB olabilir.");return}if(originalPhoto.startsWith("blob:"))URL.revokeObjectURL(originalPhoto);const u=URL.createObjectURL(file);setPhotoName(file.name);setOriginalPhoto(u);setCropSource(u);setLastCrop(undefined);setPhotoPreview("");setResult(null);setHintText("");setPracticeQuestions([]);setPracticeFeedback({});setVerifyText("");setError("");e.target.value=""} function applyCrop(dataUrl:string,crop:Crop){setPhotoPreview(dataUrl);setLastCrop(crop);setCropSource("");setHintText("");setError("")} function editCropAgain(){if(originalPhoto)setCropSource(originalPhoto)}
 return <main className="shell appShell"><section className="hero appHero">
 <header className="appTopbar"><div className="brandMark"><span className="brandOrb">N</span><div><strong>Neşevren</strong><small>Neş'eli Matematik • Yapay Zekâ Öğrenme Asistanı</small></div></div><span className="aiStatus">● Hazır</span></header>
 <div className="aiHero"><div className="aiGlow">✦</div><div><h1>Sorunu gönder.<br/>Dersi ben anlayayım.</h1><p>Fotoğraf, yazı veya ses. Neşevren içeriği analiz eder, dersi ve konuyu algılar.</p></div></div>
 <div className="quickActions compactQuick"><button className="quickAction primaryQuick" onClick={choosePhoto}><span>📷</span><strong>Fotoğrafla Sor</strong><small>Çek veya galeriden seç</small></button><button className="quickAction" onClick={()=>questionInput.current?.focus()}><span>✍️</span><strong>Yazarak Sor</strong><small>Sorunu doğrudan yaz</small></button><button className={`quickAction ${voiceActive?"voiceActive":""}`} onClick={startVoice}><span>{voiceActive?"🔴":"🎙️"}</span><strong>{voiceActive?"Dinliyorum…":"Sesli Sor"}</strong><small>{voiceActive?"Konuşmaya devam et":"Mikrofonla anlat"}</small></button></div>
 <div className="subjectAssist"><button type="button" onClick={()=>setShowSubject(v=>!v)}>🤖 {subjectLabel} <span>{showSubject?"⌃":"⌄"}</span></button>{showSubject&&<div className="subjectInline"><label htmlFor="subject">İstersen dersi elle seç — Neşevren yine de gerçek dersi kontrol eder.</label><select id="subject" value={subject} onChange={e=>setSubject(e.target.value)}>{subjects.map(s=><option key={s}>{s}</option>)}</select></div>}</div>
 <div className="questionBox modernQuestionBox"><div className="composerTitle"><strong>✦ Neşevren'e sor</strong><span>{subjectLabel}</span></div><textarea ref={questionInput} value={question} onChange={e=>setQuestion(e.target.value)} placeholder="Sorunu buraya yaz, fotoğraf ekle veya sesli anlat..." rows={4}/>{photoPreview&&<div className="photoPreview"><img src={photoPreview} alt="Kırpılmış soru fotoğrafı"/><div className="photoPreviewFooter"><span>{photoName}</span><button type="button" className="secondary" onClick={editCropAgain}>✂️ Kırpmayı düzenle</button></div></div>}<input ref={photoInput} type="file" accept="image/jpeg,image/png,image/webp,image/heic,image/heif" onChange={onPhotoChange} style={{display:"none"}}/><div className="composerActions"><button className="attachButton" onClick={choosePhoto}>＋ Fotoğraf</button><button className="solveButton" onClick={submitQuestion} disabled={busy}>{loading?"Analiz ediyorum…":"Çöz →"}</button></div></div>
 <div className="smartActions"><button className="smartAction" onClick={requestHint} disabled={busy}><span className="smartIcon">💡</span><strong>{hintLoading?"Hazırlanıyor":"İpucu Al"}</strong><small>Cevabı görmeden yönlen.</small></button><button className="smartAction" onClick={requestSimilar} disabled={busy}><span className="smartIcon">📝</span><strong>{similarLoading?"Üretiliyor":"3 Soru Hazırla"}</strong><small>{practiceGrade?`${practiceLevelText} · ${practiceOptionCount} şıklı`:"Seviye yoksa standart 4 şık"}</small></button><button className="smartAction" onClick={()=>setVerifyOpen(v=>!v)} disabled={busy}><span className="smartIcon">✓</span><strong>Doğrula</strong><small>Kendi çözümünü kontrol et.</small></button></div>
 <button className="assessmentStrip" onClick={()=>setAssessmentOpen(true)}><span>🎯</span><div><strong>Seviyemi Belirle</strong><small>{assessmentResult?`${assessmentResult.subject} · ${assessmentLevelLabel(assessmentResult.grade)} · ${assessmentResult.level}`:"Ders, sınıf ve konu seçerek seviyeni ölç."}</small></div><b>→</b></button>
 <button className="assessmentStrip" onClick={()=>{setErrorBookOpen(true);setErrorRetryId(null);setErrorRetrySelected(null)}}><span>📕</span><div><strong>Hata Kitapçığı</strong><small>{errorBook.length?`${errorBook.length} yanlış soru kişisel hafızada · konu konu tekrar et.`:"Yanlış yaptığın sorular burada birikecek."}</small></div><b>→</b></button>
 {hintText&&<div className="smartResult"><div className="smartResultHeader"><strong>💡 İpucu</strong><span>Cevap gizli</span></div><p>{hintText}</p></div>}
 {practiceQuestions.length>0&&<div className="practiceSet"><div className="practiceSetTitle"><div><strong>📝 3 Soruluk Mini Pratik</strong><span>{practiceLevelText} · {practiceOptionCount} şık · aynı beceri</span></div><b>{Object.keys(practiceFeedback).length}/3</b></div>{practiceQuestions.map((item,qIndex)=>{const feedback=practiceFeedback[qIndex];return <article className="practiceCard" key={`${qIndex}-${item.question}`}><div className="practiceNumber">{qIndex+1}</div><h3>{item.question}</h3><div className="practiceOptions">{item.options.map((option,oIndex)=>{const selected=feedback?.selected===oIndex;const isCorrectOption=feedback&&oIndex===item.correctIndex;const cls=feedback?(isCorrectOption?"correct":selected?"wrong":"muted"):"";return <button key={`${oIndex}-${option}`} className={cls} onClick={()=>selectPracticeOption(qIndex,oIndex)} disabled={Boolean(feedback)}><span>{String.fromCharCode(65+oIndex)}</span>{option}</button>})}</div>{feedback&&<div className={`practiceFeedback ${feedback.correct?"success":"retry"}`}>{feedback.correct?<><strong>🎉 Tebrikler, doğru cevap!</strong><span>Harika gidiyorsun.</span></>:<><strong>Bir daha düşünelim.</strong><span>💡 {item.hint}</span></>}</div>}</article>})}</div>}
 {verifyOpen&&<div className="smartResult"><div className="smartResultHeader"><strong>✓ Çözümünü doğrula</strong><span>Geri bildirim</span></div><div className="verifyBox"><textarea value={studentAnswer} onChange={e=>setStudentAnswer(e.target.value)} placeholder="Kendi cevabını veya çözüm adımlarını yaz..."/><button onClick={verifyStudentAnswer} disabled={busy}>{verifyLoading?"Kontrol ediyorum…":"Kontrol Et"}</button></div>{verifyText&&<p>{verifyText}</p>}</div>}
 <button className="teacherStrip" onClick={()=>{setTeacherOpen(true);setTeacherSent(false)}}><span>👩‍🏫</span><div><strong>Canlı öğretmen ister misin?</strong><small>Sorunu kayıtlı branş öğretmenine gönder.</small></div><b>→</b></button>
 {history.length>0&&<div className="historyStrip"><strong>Son çalıştıkların</strong><div>{history.map((h,i)=><button key={`${h.createdAt}-${i}`} onClick={()=>{setQuestion(h.question);setSubject(h.subject);questionInput.current?.focus()}}>{h.subject==="Otomatik"?"AI":h.subject} · {h.question}</button>)}</div></div>}
 {loading&&<div className="thinking"><span>✦</span><div><strong>Neşevren analiz ediyor…</strong><small>Gerçek ders, konu ve çözüm yolunu belirliyorum.</small></div></div>}{error&&<div className="errorBox">{error}</div>}{result&&<SolutionResult result={result}/>}</section>
 <nav className="bottomNav"><button className="active">⌂<small>Ana Sayfa</small></button><button type="button" data-nesevren-coach="true" aria-label="Koç Hizmeti Al">◎<small>Koç Hizmeti Al</small></button><button onClick={()=>window.scrollTo({top:document.body.scrollHeight,behavior:"smooth"})}>○<small>Geçmiş</small></button></nav>
 {assessmentOpen&&<div className="assessmentBackdrop" onClick={()=>setAssessmentOpen(false)}><section className="assessmentModal" onClick={e=>e.stopPropagation()}><button className="assessmentClose" onClick={()=>setAssessmentOpen(false)}>×</button><div className="assessmentHead"><span>🎯</span><div><h2>Seviyemi Belirle</h2><p>Ders, sınıf ve konuya göre seviyene uygun {assessmentQuestionCount} soruluk tanılama.</p></div></div>{assessmentQuestions.length===0&&!assessmentResult&&<div className="assessmentStart"><label>Sınıf / Seviye<select value={assessmentGrade} onChange={e=>setAssessmentGrade(e.target.value)}>{grades.map(g=><option key={g} value={g}>{/^\d+$/.test(g)?`${g}. Sınıf`:g}</option>)}</select></label><label>Ders<select value={assessmentSubject} onChange={e=>setAssessmentSubject(e.target.value)}>{assessmentSubjects.map(s=><option key={s}>{s}</option>)}</select></label><label>Konu / Tema<select value={assessmentTopic} onChange={e=>setAssessmentTopic(e.target.value)}>{getAssessmentTopics(assessmentSubject,assessmentGrade).map(t=><option key={t}>{t}</option>)}</select></label><div className="teacherNotice">Bu seviyede test <strong>{assessmentQuestionCount} soru</strong> ve her soru <strong>{assessmentOptionCount} şıklı</strong> hazırlanır.</div><button onClick={startAssessment} disabled={assessmentLoading}>{assessmentLoading?"Soruları hazırlıyorum…":"Testi Başlat"}</button></div>}{assessmentQuestions.length>0&&!assessmentResult&&<div className="assessmentQuestions"><div className="assessmentProgress"><span>{Object.keys(assessmentAnswers).length}/{assessmentQuestionCount} cevaplandı · {assessmentOptionCount} şık</span><i style={{width:`${(Object.keys(assessmentAnswers).length/assessmentQuestionCount)*100}%`}}/></div>{assessmentQuestions.map((item,index)=>{const selected=assessmentAnswers[index];return <article className="assessmentQuestion" key={`${index}-${item.question}`}><small>{assessmentSubject} · {assessmentLevelLabel(assessmentGrade)} · {item.topic}</small><h3>{index+1}. {item.question}</h3><div>{item.options.map((option,oIndex)=>{const answered=selected!==undefined;const isSelected=selected===oIndex;const correct=answered&&oIndex===item.correctIndex;const cls=answered?(correct?"correct":isSelected?"wrong":"muted"):"";return <button className={cls} disabled={answered} onClick={()=>chooseAssessmentAnswer(index,oIndex)} key={`${oIndex}-${option}`}><b>{String.fromCharCode(65+oIndex)}</b>{option}</button>})}</div>{selected!==undefined&&selected!==item.correctIndex&&<p>💡 {item.hint}</p>}</article>})}</div>}{assessmentResult&&<div className="assessmentSummary"><div className="assessmentScore"><strong>{assessmentResult.score}/{assessmentResult.total}</strong><span>{assessmentResult.level}</span></div><h3>{assessmentResult.subject} · {assessmentLevelLabel(assessmentResult.grade)} · {assessmentResult.topic}</h3>{assessmentResult.topicStats&&<><p>Konu bazlı doğru / yanlış analizi:</p><div className="weakTopics">{Object.entries(assessmentResult.topicStats).map(([topic,stat])=><span key={topic}>{topic}: ✅ {stat.correct} · ❌ {stat.wrong}</span>)}</div></>}{assessmentResult.weakTopics.length>0?<><p>Yanlış yaptığın sorular Hata Kitapçığı'na kaydedildi. Öncelikle şu alanları güçlendir:</p><div className="weakTopics">{assessmentResult.weakTopics.map(t=><span key={t}>{t}</span>)}</div></>:<p>Bu konuda güçlü görünüyorsun. Daha zor sorularla pekiştirebilirsin. 🎉</p>}<button onClick={resetAssessment}>Testi Yenile</button></div>}</section></div>}
 {errorBookOpen&&<div className="assessmentBackdrop" onClick={()=>setErrorBookOpen(false)}><section className="assessmentModal" onClick={e=>e.stopPropagation()}><button className="assessmentClose" onClick={()=>setErrorBookOpen(false)}>×</button><div className="assessmentHead"><span>📕</span><div><h2>Hata Kitapçığı</h2><p>Yanlışların ders, sınıf ve konuya göre kişisel hafızada tutulur. Konuyu çalıştıktan sonra aynı soruyu veya benzerlerini tekrar çöz.</p></div></div>{!errorRetryItem?<>{errorGroups.length===0?<div className="teacherNotice">Henüz kayıtlı hata yok. Seviye testinde yanlış yaptığın sorular otomatik olarak buraya eklenecek.</div>:errorGroups.map(([group,items])=><article className="assessmentQuestion" key={group}><small>{group}</small><h3>{items.length} kayıtlı hata</h3>{items.map(item=><div key={item.id} className="teacherNotice"><strong>{item.question}</strong><p>Bu soruda toplam {item.mistakeCount} kez hata kaydı var. Tekrar: ✅ {item.retryCorrect} · ❌ {item.retryWrong}</p><div className="composerActions"><button onClick={()=>{setErrorRetryId(item.id);setErrorRetrySelected(null)}}>Aynısını Tekrar Çöz</button><button onClick={()=>generateErrorSimilar(item)} disabled={errorPracticeLoading}>{errorPracticeLoading?"Hazırlanıyor…":"3 Benzer Soru"}</button></div></div>)}</article>)}</>:<article className="assessmentQuestion"><small>{errorRetryItem.subject} · {errorRetryItem.grade} · {errorRetryItem.topic}</small><h3>{errorRetryItem.question}</h3><div>{errorRetryItem.options.map((option,oIndex)=>{const answered=errorRetrySelected!==null;const selected=errorRetrySelected===oIndex;const correct=answered&&oIndex===errorRetryItem.correctIndex;const cls=answered?(correct?"correct":selected?"wrong":"muted"):"";return <button key={`${oIndex}-${option}`} className={cls} disabled={answered} onClick={()=>retryErrorAnswer(oIndex)}><b>{String.fromCharCode(65+oIndex)}</b>{option}</button>})}</div>{errorRetrySelected!==null&&(errorRetrySelected===errorRetryItem.correctIndex?<p>🎉 Doğru. Bu hatayı artık daha iyi tanıyorsun.</p>:<p>💡 Yanlış. {errorRetryItem.hint}</p>)}<div className="composerActions"><button onClick={()=>{setErrorRetryId(null);setErrorRetrySelected(null)}}>Hata Listesine Dön</button><button onClick={()=>generateErrorSimilar(errorRetryItem)} disabled={errorPracticeLoading}>{errorPracticeLoading?"Hazırlanıyor…":"Benzerlerini Çöz"}</button></div></article>}</section></div>}
 {teacherOpen&&<div className="teacherModalBackdrop" onClick={()=>setTeacherOpen(false)}><div className="teacherModal" onClick={e=>e.stopPropagation()}><button className="teacherClose" onClick={()=>setTeacherOpen(false)}>×</button><span className="teacherBigIcon">👩‍🏫</span><h2>Canlı Öğretmen Talebi</h2>{teacherSent?<><p>Talebin hazırlandı. Öğretmen kuyruğu ve ödeme altyapısı daha sonra bağlanacak.</p><button className="solveButton" onClick={()=>setTeacherOpen(false)}>Tamam</button></>:<div className="teacherRequest"><label>Ders<select value={subject} onChange={e=>setSubject(e.target.value)}>{subjects.filter(s=>s!=="Otomatik").map(s=><option key={s}>{s}</option>)}</select></label><label>Soru / not<textarea value={question} onChange={e=>setQuestion(e.target.value)} placeholder="Öğretmene iletilecek kısa not..."/></label><button onClick={()=>setTeacherSent(true)}>Talebi Hazırla</button><div className="teacherNotice">Ücret tahsilatı ve otomatik öğretmen ataması henüz aktif değildir.</div></div>}</div></div>}
 {celebration!==null&&<div className="confettiLayer" aria-hidden="true">{Array.from({length:28}).map((_,i)=><i key={i} style={{left:`${(i*37)%100}%`,animationDelay:`${(i%7)*.08}s`,animationDuration:`${1.1+(i%5)*.12}s`}} />)}</div>}
 {cropSource&&<PhotoCropper src={cropSource} initialCrop={lastCrop} onCancel={()=>setCropSource("")} onApply={applyCrop}/>}</main>}
createRoot(document.getElementById("root")!).render(<React.StrictMode><App/></React.StrictMode>);
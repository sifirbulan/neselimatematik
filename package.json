{
  "name": "neseli-matematik-ai",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["apps/*", "services/*"],
  "scripts": {
    "dev:web": "npm --workspace apps/web run dev",
    "dev:api": "npm --workspace services/api run dev",
    "build": "npm run build --workspaces",
    "test": "npm test --workspaces"
  }
}

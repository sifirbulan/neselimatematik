import OpenAI from "openai";
import type { AIAnswer, ProviderExecutor, QuestionAnalysis, StudentQuestion } from "./types.js";
import { parseVisualization, visualizationGuidance } from "./visualization.js";

function buildPrompt(input:StudentQuestion,analysis:QuestionAnalysis):string{const parts=["Sen Neşevren adlı yapay zekâ destekli çok dersli öğrenme platformunun eğitim motorusun.","Sorunun ait olduğu alanı doğru yorumla; matematik, fen, fizik, kimya, biyoloji, Türkçe, edebiyat, İngilizce, Kürtçe, sosyal bilgiler, tarih, coğrafya ve felsefe sorularında alanın kurallarına uygun cevap ver.","Türkçe, açık, pedagojik, öğrenci seviyesine uygun ve olgusal olarak doğru cevap ver. Dil sorularında gerektiğinde hedef dilde örnek kullan.","Bilmediğin veya görselden okuyamadığın ayrıntıyı uydurma.",visualizationGuidance,"Cevabı yalnızca geçerli JSON olarak döndür. Alanlar: answer, explanation, steps, hint, visualization.",`İstek türü: ${input.intent}`,`Konu: ${analysis.topic} / ${analysis.subtopic}`,`Sınav: ${analysis.exam}`,`Zorluk: ${analysis.difficulty}`];if(input.inputType==="image")parts.push("Ekli kırpılmış görselde yalnızca seçili soruyu oku; metin, sayı, seçenek, sembol, şekil ve tabloları dikkatle yorumla. Geometri şekli önemliyse visualization alanında yalnızca görselde bulunan verilerle şekli yeniden kur. Okunmuyorsa açıkça söyle.");if(input.question)parts.push(`Öğrencinin sorusu ve ek notu: ${input.question}`);return parts.join("\n")}
function parseAnswer(raw:string):AIAnswer{let value:unknown;try{value=JSON.parse(raw)}catch{throw new Error("Groq yapılandırılmış bir cevap döndürmedi.")}if(!value||typeof value!=="object")throw new Error("Groq cevabı geçersiz.");const data=value as Record<string,unknown>;if(typeof data.answer!=="string"||typeof data.explanation!=="string"||!Array.isArray(data.steps)||!data.steps.every(i=>typeof i==="string"))throw new Error("Groq cevabında zorunlu alanlar eksik.");return{answer:data.answer,explanation:data.explanation,steps:data.steps,hint:typeof data.hint==="string"&&data.hint?data.hint:undefined,visualization:parseVisualization(data.visualization),verified:false,verificationStatus:"pending",confidence:.8}}
export const groqExecutor:ProviderExecutor={providerId:"groq",async execute(input,analysis){const apiKey=process.env.GROQ_API_KEY?.trim();if(!apiKey)throw new Error("GROQ_API_KEY tanımlı değil.");const isImage=input.inputType==="image"&&Boolean(input.imageDataUrl);const model=isImage?(process.env.GROQ_VISION_MODEL?.trim()||"qwen/qwen3.6-27b"):(process.env.GROQ_MODEL?.trim()||"openai/gpt-oss-120b");const client=new OpenAI({apiKey,baseURL:"https://api.groq.com/openai/v1"});const prompt=buildPrompt(input,analysis);const messages=isImage?[{role:"user" as const,content:[{type:"text" as const,text:prompt},{type:"image_url" as const,image_url:{url:input.imageDataUrl!}}]}]:[{role:"user" as const,content:prompt}];try{const response=await client.chat.completions.create({model,messages,response_format:{type:"json_object"},temperature:.2});const output=response.choices[0]?.message?.content?.trim();if(!output)throw new Error("Groq boş cevap döndürdü.");return parseAnswer(output)}catch(error){const message=error instanceof Error?error.message:"Bilinmeyen Groq hatası";throw new Error(`${isImage?"Groq görsel":"Groq metin"} çağrısı başarısız: ${message}`)}}};
